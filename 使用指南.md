# Binance PUMP 检测器 - 详细使用指南

> 本指南面向初学者，手把手教你如何使用本项目

## 目录

- [什么是这个项目？](#什么是这个项目)
- [它有什么用？](#它有什么用)
- [开始前需要准备什么](#开始前需要准备什么)
- [安装步骤](#安装步骤)
- [配置 Binance API](#配置-binance-api)
- [运行程序](#运行程序)
- [理解输出内容](#理解输出内容)
- [常见问题](#常见问题)
- [注意事项](#注意事项)

---

## 什么是这个项目？

这是一个 **币安（Binance）交易监控工具**，专门用来检测加密货币市场中的异常交易活动。

简单来说，它会：
1. 连接到币安的实时交易数据流
2. 收集各种交易对（比如 BTC/USDT）的价格、成交量等信息
3. 分析这些数据，找出异常的价格或成交量变化
4. 把这些"异常"展示给你看

**为什么异常重要？**

在加密货币市场中，突然的价格暴涨或暴跌（俗称"拉盘"或"砸盘"）往往是：
- 🚀 潜在的赚钱机会
- ⚠️ 市场风险信号
- 📊 重要的交易参考信息

## 它有什么用？

- **监控市场**：实时查看哪些币种交易最活跃
- **发现机会**：快速识别价格或成交量异常变化的币种
- **数据分析**：帮助了解市场热点和资金流向

---

## 开始前需要准备什么

### 1. 一台电脑
- Windows、macOS 或 Linux 都可以
- 需要能连接互联网

### 2. Python 环境
本项目需要 **Python 3.7 或更高版本**

**如何检查是否已安装 Python？**

打开命令行（终端）：
- Windows: 按 `Win + R`，输入 `cmd`，回车
- macOS: 按 `Cmd + Space`，输入 `terminal`，回车
- Linux: 按 `Ctrl + Alt + T`

输入以下命令：
```bash
python3 --version
```

如果看到类似 `Python 3.x.x` 的输出，说明已经安装了 ✅

如果没有安装，请访问 [python.org](https://www.python.org/downloads/) 下载安装

### 3. 币安账户
你需要一个币安账户来获取 API 密钥
- 注册地址：[binance.com](https://www.binance.com)
- 不需要资金，只需要注册账号即可

---

## 安装步骤

### 第一步：下载项目

你可以通过两种方式获取项目代码：

**方式一：使用 Git（推荐）**

打开命令行，进入你想保存项目的目录，然后执行：
```bash
git clone https://github.com/ogu83/binancePump.git
cd binancePump
```

**方式二：手动下载**

1. 访问项目页面：https://github.com/ogu83/binancePump
2. 点击绿色的 "Code" 按钮
3. 选择 "Download ZIP"
4. 下载后解压到任意目录
5. 打开解压后的文件夹

### 第二步：安装依赖库

在项目目录中，打开命令行，执行：

```bash
pip3 install -r requirements.txt
```

**什么是 pip？**

`pip` 是 Python 的包管理工具，用来安装额外的库。上面的命令会自动安装项目所需的所有依赖。

**如果遇到权限问题**（macOS/Linux），可以尝试：
```bash
pip3 install -r requirements.txt --user
```

### 第三步：安装额外依赖

有些依赖在 `requirements.txt` 中没有列出，但程序运行时需要。执行：

```bash
pip3 install dateparser pytz python-binance pyTelegramBotAPI tqdm
```

**等待安装完成**

安装过程可能需要几分钟，取决于你的网络速度。看到 `Successfully installed` 就表示成功了 ✅

---

## 配置 Binance API

### 什么是 API 密钥？

API 密钥就像一把"钥匙"，让你的程序可以访问币安的数据。

### 如何获取 API 密钥？

1. **登录币安账户**
   - 访问 [binance.com](https://www.binance.com)
   - 使用你的账号密码登录

2. **进入 API 管理**
   - 点击右上角的头像图标
   - 选择 "API Management"（API 管理）

3. **创建新的 API 密钥**
   - 点击 "Create API"（创建 API）
   - 给你的 API 起个名字（比如 "PumpDetector"）
   - 可能需要输入二次验证码

4. **复制密钥**
   - 你会看到两个字符串：
     - **API Key**（公钥）
     - **Secret Key**（私钥，只显示一次！**一定要复制保存**）
   
   ⚠️ **重要提醒**：
   - Secret Key 只显示一次，**请立即复制并保存好**
   - 不要分享给你的任何人
   - 建议记录在安全的地方

5. **设置权限（可选但推荐）**
   - 为了安全，只勾选 "Enable Reading"（启用读取权限）
   - 不要勾选 "Enable Withdrawals"（启用提现权限）

### 配置项目

打开项目目录中的 `api_config.json` 文件，你会看到：

```json
{ 
    "api_key": "",
    "api_secret": ""
}
```

将你的密钥填入（注意保持引号）：

```json
{ 
    "api_key": "你的API_Key_在这里",
    "api_secret": "你的Secret_Key_在这里"
}
```

保存文件。

**小贴士**：
- 注意不要删除引号 `""`
- 确保在两个密钥之间有逗号 `,`
- 最后一行的大括号 `}` 后面不要加逗号

---

## 运行程序

### 启动程序

在项目目录中打开命令行，输入：

```bash
python3 binancePump.py
```

你会看到类似这样的输出：

```
Websocket running. Press Ctrl+C to exit.
```

这表示程序已经开始运行了 ✅

### 停止程序

按 `Ctrl + C`（同时按住 Ctrl 键和 C 键）即可停止程序

程序会显示：

```
Shutting down…
Clean exit complete.
```

---

## 理解输出内容

程序运行后会持续监控市场，当检测到异常时会打印信息。

### 输出格式示例

```
Top Ticks
Symbol:BTCUSDT    Time:2025-02-01 12:30:45    Ticks:15    RPCh:3.45    TPCh:8.92    VCh:12.34    LP:43500.50    LV:1234.56

Top Total Price Change
Symbol:ETHUSDT    Time:2025-02-01 12:30:45    Ticks:8    RPCh:5.67    TPCh:15.23    VCh:20.11    LP:2340.80    LV:5678.90
```

### 字段说明

| 字段 | 含义 | 说明 |
|------|------|------|
| **Symbol** | 交易对名称 | 比如 `BTCUSDT` 表示比特币/泰达币交易对 |
| **Time** | 时间戳 | 最后一次检测到异常的时间 |
| **Ticks** | 触发次数 | 该交易对达到异常阈值被检测到的次数 |
| **RPCh** | 相对价格变化 | 累计的价格变化百分比（正数=涨，负数=跌） |
| **TPCh** | 总价格变化 | 累计价格变化的绝对值总和 |
| **VCh** | 成交量变化 | 成交量变化的百分比 |
| **LP** | 最新价格 | 当前的最新价格 |
| **LV** | 最新成交量 | 当前的成交量 |

### 四种排行榜

程序会显示四种不同的排行榜：

1. **Top Ticks** - 触发次数最多的币种
2. **Top Total Price Change** - 价格变化最大的币种
3. **Top Relative Price Change** - 相对价格变化最大的币种
4. **Top Total Volume Change** - 成交量变化最大的币种

---

## 配置选项

在 `binancePump.py` 文件的开头，有几个配置参数可以调整：

### 只监控特定交易对

```python
show_only_pair = "USDT"  # 只显示包含 USDT 的交易对
```

如果你想监控所有交易对，改为：
```python
show_only_pair = ""  # 空字符串表示监控所有
```

### 显示数量限制

```python
show_limit = 1  # 每个排行榜只显示前 1 个
```

如果你想看更多，可以改成 5 或 10。

### 最小变化阈值

```python
min_perc = 0.05  # 最小变化百分比（5%）
```

只有当价格或成交量变化超过 5% 时才会被检测到。

**注意**：修改配置后需要重启程序才能生效。

---

## 常见问题

### Q1: 运行时提示 "ModuleNotFoundError"

**问题**：
```
ModuleNotFoundError: No module named 'xxx'
```

**解决方法**：
某个依赖库没有安装成功，执行：
```bash
pip3 install xxx
```
把 `xxx` 替换成缺失的模块名。

### Q2: 连接币安时出现错误

**问题**：提示连接失败或 API 错误

**可能原因**：
1. API 密钥配置错误
2. 网络连接问题
3. 币安服务器暂时不可用

**解决方法**：
1. 检查 `api_config.json` 中的密钥是否正确
2. 检查网络连接
3. 等待几分钟后重试

### Q3: 没有任何输出

**问题**：程序运行但没有显示任何信息

**原因**：
- 当前市场没有达到阈值的异常活动
- 配置的阈值太高

**解决方法**：
尝试降低 `min_perc` 的值，比如改成 `0.01`（1%）

### Q4: Python 版本太旧

**问题**：提示 Python 版本不兼容

**解决方法**：
升级 Python 到 3.7 或更高版本

### Q5: macOS 提示权限问题

**问题**：
```
Permission denied: '/usr/local/bin/xxx'
```

**解决方法**：
```bash
pip3 install --user -r requirements.txt
```

---

## 注意事项

### 安全提醒

⚠️ **非常重要**：
- **不要** 将 `api_config.json` 上传到公开的代码仓库
- **不要** 分享你的 API 密钥给任何人
- **定期更换** API 密钥
- **不要** 勾选 API 的提现权限

本项目已经将 `api_config.json` 添加到 `.gitignore`，不会被提交到 Git。

### 使用限制

- 币安 API 有访问频率限制（rate limit），不要频繁重启程序
- 只用于个人学习和研究，不要用于商业用途

### 性能建议

- 长时间运行时注意电脑性能和内存使用
- 如果监控的交易对很多，可能需要更多的系统资源

---

## 高级使用（可选）

### 自定义监控规则

如果你想修改检测逻辑，可以编辑 `binancePump.py` 中的 `process_message` 函数。

### 集成 Telegram Bot

项目支持发送通知到 Telegram Bot，这需要额外的配置。详情可以查看代码中的相关部分。

---

## 获取帮助

如果遇到问题：

1. **查看本文档**：先在常见问题部分找找答案
2. **查看代码注释**：代码中有详细的注释说明
3. **搜索网络**：使用错误信息在搜索引擎中搜索
4. **查看币安 API 文档**：[https://binance-docs.github.io/apidocs/](https://binance-docs.github.io/apidocs/)

---

## 项目文件说明

| 文件名 | 说明 |
|--------|------|
| `binancePump.py` | 主程序，包含核心逻辑 |
| `binanceHelper.py` | 辅助函数，处理币安 API 相关操作 |
| `pricechange.py` | 价格变化数据类 |
| `pricegroup.py` | 价格分组数据类 |
| `requirements.txt` | Python 依赖列表 |
| `api_config.json` | API 密钥配置（需要自己填写） |

---

## 更新日志

- 项目持续更新中，关注 GitHub 获取最新版本

---

## 许可证

请查看项目根目录下的 LICENSE 文件（如有）

---

**祝使用愉快！如有问题欢迎反馈 🚀**
